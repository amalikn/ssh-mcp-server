# Capability Overview
This repository supports external local metadata capture under the canonical data root.
Agents should consume repo-safe capability docs from this folder and operational local facts from external `meta/`.
